/**
 * Common module containing shared utilities, DTOs, and configurations
 * used across all other modules.
 *
 * This module is explicitly defined as shared in the @Modulith annotation
 * and can be accessed by all other modules without violating modularity principles.
 *
 * Contains:
 * - Base entities and DTOs
 * - Security utilities
 * - Exception handling
 * - Common validators
 * - API response wrappers
 */
package com.smiles.common;
